from __future__ import annotations
from typing import TYPE_CHECKING
if not TYPE_CHECKING:
    from survey import Answer, Survey, Question


def test_sort_students(lst: List[Student], attribute: str) -> bool:
    pass
